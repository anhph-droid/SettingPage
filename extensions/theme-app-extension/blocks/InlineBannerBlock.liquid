{% liquid
  assign settings_banner_block_root_id = 'settings-banner-inline-block-' | append: block.id
%}

{% render 'settings-banner-content',
  root_id: settings_banner_block_root_id,
  include_block_attributes: true,
  banner_id: block.settings.banner_id,
  render_mode: 'section',
  force_size: 'inline'
%}

{% schema %}
{
  "name": "Inline Banner",
  "target": "section",
  "settings": [
    {
      "type": "text",
      "id": "banner_id",
      "label": "Banner ID",
      "info": "Nhập ID banner muốn hiển thị theo kiểu Inline."
    }
  ]
}
{% endschema %}
