
{% liquid
  assign settings_banner_embed_root_id = 'settings-banner-embed-' | append: block.id
%}

{% render 'settings-banner-content',
  root_id: settings_banner_embed_root_id,
  include_block_attributes: false
%}

{% schema %}
{
  "name": "Settings Banner Embed",
  "target": "body",
  "settings": []
}
{% endschema %}
